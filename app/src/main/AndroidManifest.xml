<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mytest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Comvus"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTest">
        <activity android:name=".RewardPageActivity"></activity>
        <activity android:name=".SendPointsActivity" />
        <activity android:name=".DistributePointsActivity" />
        <activity android:name=".CreateCommentActivity" />
        <activity android:name=".CommentActivity" />
        <activity android:name=".CreatePostActivity" />
        <activity android:name=".ForumActivity" />
        <activity android:name=".DiscussionCategoryActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".GroupMessageActivity" />
        <activity android:name=".CreateGroup" />
        <activity android:name=".MessageActivity" />
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".StartActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".StartActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>